# CS-001 v3 — Execute

## Caminho local

```powershell
cd C:\Projetos\connectioncyber\cc-commerce-studio
```

## Aplicação

Copie o conteúdo do pacote para:

```text
C:\Projetos\connectioncyber\cc-commerce-studio
```

Substitua os arquivos existentes quando solicitado.

## Executar

```powershell
npm run dev
```

## Validar

Abra:

```text
http://localhost:3000
```

## Commit após validação

```powershell
git status
git add .
git commit -m "style: align Commerce Studio visual standard"
git push -u origin main
```

Se o remoto já estiver configurado:

```powershell
git push
```
