// Superado por `authAction` em `sign-in.action.ts`, que trata sign-in e
// sign-up na mesma Server Action via campo "intent". Mantido vazio para
// não quebrar histórico; não importar deste arquivo.
export {};
